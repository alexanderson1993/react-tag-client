mutation JoinGame($gameId: ID!) {
  startGame(gameId: $gameId) {
    game_id
    name
    description
    code
    owner {
      user_id
      name
    }
    started
    completed
    winner {
      user_id
      name
      photoURL
    }
    me {
      id
      dead
      target {
        id
        user {
          user_id
          name
          photoURL
        }
      }
    }
    playerCount
    aliveCount
  }
}
